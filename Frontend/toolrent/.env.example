# Modo desarrollo: sin Keycloak
VITE_USE_KEYCLOAK=false

# Usar datos de prueba (mock) cuando el backend no está disponible
VITE_USE_MOCK_DATA=true

# URL base del API backend
VITE_API_BASE_URL=https://toolrent.duckdns.org/api/v1

# Configuración de Keycloak (no se usa en desarrollo, pero se define para evitar errores)
VITE_KEYCLOAK_URL=http://localhost:8080
VITE_KEYCLOAK_REALM=toolrent
VITE_KEYCLOAK_CLIENT_ID=toolrent-app
